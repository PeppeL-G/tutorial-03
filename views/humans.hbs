<h1>All humans</h1>

{{#if somethingWentWrong}}
	<p>Internal server error...</p>
{{else}}
	
	{{#if humans.length}}
		<ul>
			{{#each humans}}
				<li><a href="/humans/{{id}}">{{name}}</a></li>
			{{/each}}
		</ul>
	{{else}}
		<p>There are no humans :(</p>
	{{/if}}
	
{{/if}}